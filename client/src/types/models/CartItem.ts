export type CartItem = {
  cartItemId: number;
  bookId: number;
  title: string;
  author: string;
  coverImageUrl: string;
  price: number;
  currency: string;
  discountPercent: number;
  addedDate: string;
};
